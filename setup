#!/bin/bash

# Function to check if a command exists
command_exists() {
    command -v "$1" >/dev/null 2>&1
}

# Check if Docker is installed
if ! command_exists docker; then
    echo "Error: Docker is not installed. Please install Docker first."
    exit 1
fi

# Copy the RFPs CSV file to the container
docker cp core/data/rfps.csv interview-core-1:/app/rfps.csv

# Run the import command inside the container
docker exec -i interview-core-1 python manage.py import_rfps rfps.csv
